[project]
name = "opa-gov-api"
version = "0.1.0"
description = "FastAPI wrapper around OPA for inline security and masking decisions"
readme = "README.md"
requires-python = ">=3.12.8"
dependencies = [
    "python-dotenv>=1.0.1",
    "fastapi>=0.116.1",
    "httpx>=0.27.0",
    "uvicorn>=0.35.0",
]
